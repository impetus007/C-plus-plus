#include <bits/stdc++.h>
#define int long long
#define ull unsigned long long
using namespace std;
signed main()
{
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    if (n == a && n == b and n == c)
    {
        cout << "1\n";
    }
    else
    {
        cout << "2\n";
    }

    return 0;
}
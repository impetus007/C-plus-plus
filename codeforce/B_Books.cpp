#include <bits/stdc++.h>
#define int long long
#define ull unsigned long long
using namespace std;
signed main()
{
    int a, b;
    cin >> a >> b;
    vector<int> v;
    for (int i = 0; i < a; i++)
    {
        int x;
        cin >> x;
        v.push_back(x);
    }
    //sort(v.begin(), v.end());
    
    return 0;
}
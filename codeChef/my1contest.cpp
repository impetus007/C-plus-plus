#include <bits/stdc++.h>
#define int long long
#define ull unsigned long long
using namespace std;
signed main()
{
    int n, a, b;
    cin >> n >> a >> b;

    cout << (n - a) << " " << (n - a - b) << "\n";
    return 0;
}